This is a [Next.js](https://nextjs.org) project bootstrapped with [`create-next-app`](https://nextjs.org/docs/app/api-reference/cli/create-next-app).

## Getting Started

First, run the development server:

```bash
npm run dev
# or
yarn dev
# or
pnpm dev
# or
bun dev
```

Node v20.19.0

deploy máy khách
npm install 
npm run build
npx serve out


hoac co the

npm run build
npm run start

Có thể dùng PM2 để tự khởi động cùng máy:
npm i -g pm2
pm2 start "npm run start" --name my-next
pm2 save && pm2 startup